export type SectionType = "ABOUT" | "FEATURES" | "HERO" | "SUBSCRIBE";

export const SectionType = {
    ABOUT: "ABOUT" as SectionType,
    FEATURES: "FEATURES" as SectionType,
    HERO: "HERO" as SectionType,
    SUBSCRIBE: "SUBSCRIBE" as SectionType,
};
